  
RAG systems are often deployed in contexts where the end user _expects to interact with an LLM in a conversational manner_, as though they're chatting with another person. As a result, _human-written LLM prompts make for bad search queries_.

The idea is to parse the prompt to identify the **intent** and edit, rewrite, or entirely transform the prompt to optimize it for retrieval.

---
## Techniques

### 1. Query Rewriting

- Removes unnecessary information.
- Removes ambiguity.
- Benefits are substantial, justifying costs easily.

![[Pasted image 20260602082043.png]]

This technique is useful for 95% of cases as is the implementation that adds less latency.

### 2. Named Entity Recognition

### 3. Hypothetical Document Embedding (HyDE)

Uses generated "_Hypothetical Documents_" that would be an ideal search to help with the search process.

- Adds latency and cost in terms of tokens.
- Could improve performance but not worth for the price.

![[Pasted image 20260602091115.png]]