The specification is a precise, descriptive and structured description of how a system should behave. The _goal_ of the specification is to reduce ambiguity and left less space to model interpretations.

**What a Good Specification Should Contain?**

1. Context -> _Why_ this functionality exists and _which_ problem(s) solve?
2. Boundaries -> What _is_ and _is not_ covered by the functionality (_NEVER LEAVE THIS IMPLICIT_).
3. Inputs -> _What_ data is received by the application (using format, restrictions and required data is important).
4. Outputs -
5. Behavior Rules ->
6. Restrictions ->
7. Edge Cases ->
8. Test Cases or Samples ->
9. Acceptance Criteria -> 